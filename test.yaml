id: https://example.org/test
name: test
prefixes:
  linkml: https://w3id.org/linkml/
default_prefix: test
imports:
  - linkml:types
  - ./ref
classes:
  NewClass:
    is_a: RefClass
    tree_root: true
  NewClass1:
    attributes:
      test:
        range: RefClass
