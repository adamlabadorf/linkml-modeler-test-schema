id: https://example.org/test
name: test
prefixes:
  linkml: https://w3id.org/linkml/
default_prefix: test
imports:
  - linkml:types
  - ./ref
classes:
  NewClass:
    is_a: RefClass
    tree_root: true
    attributes:
      geneVersion:
        range: NewClass1
  NewClass1:
    is_a: gene
    attributes:
      test:
        range: RefClass
